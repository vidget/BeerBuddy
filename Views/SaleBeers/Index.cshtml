@model IEnumerable<BeerBuddy.Models.SaleBeer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BarId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BeerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BeerCost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BarId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BeerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BeerCost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SaleBeerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SaleBeerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SaleBeerId })
        </td>
    </tr>
}

</table>
